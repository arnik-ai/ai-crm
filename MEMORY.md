# 🧠 حافظه‌ی پروژه — CRM هوش‌مصنوعی‌محور آموزشی

> این فایل خلاصه‌ی همه‌ی کارهای انجام‌شده + قوانین کاری است. قبل از هر تغییر، این را بخوان.

---

## ⚠️ قوانین طلایی کار (بسیار مهم — همیشه رعایت شوند)

1. **بدون اجازه و تأیید کاربر، هیچ کدی تغییر داده نشود و هیچ فایلی حذف نشود.**
2. **مراقب شکستن کدها باش** — هر تغییر باید قبل و بعدش با compile/test بررسی شود.
3. **کدها را کامل بخوان، حدس نزن.** قبل از ویرایش هر فایل، کلش را با دقت بخوان.
4. **همیشه دقت کن** — تغییرات کوچک و دقیق، نه بازنویسی‌های بزرگ بی‌دلیل.
5. **کدها ماژولار بمانند** — معماری Clean Architecture و جداسازی ماژول‌ها حفظ شود؛
   هر قابلیت در ماژول خودش، پشت اینترفیس، بدون وابستگی‌های درهم.
6. فایل `crm/.env` (ریشه) مالِ پروژه‌ی دیگری (rankiiup) است — **دست نزن، حذف نکن، منتقل نکن.**
7. **همیشه کدها را داکیومنت کن (مستندسازی).** هر کدی که می‌نویسی یا تغییر می‌دهی باید
   توضیح داشته باشد تا بعداً قابل‌فهم باشد. این یعنی:
   - **داخل کد:** docstring فارسی برای هر کلاس/تابع + کامنت برای منطق غیربدیهی
     (چرا این‌طور، نه فقط چه‌کار). الگوی موجود همین است — حفظش کن.
   - **در MEMORY.md:** بعد از هر کارِ مهم، یک مورد به «کارهای انجام‌شده» + تاریخچه‌ی
     commit اضافه کن (چه شد، چرا، کدام فایل‌ها).
   - **کارهای قبلی هم:** اگر کدی بدون توضیح ماند، هنگام عبور از آن، مستندش کن.
8. **همه‌ی تاریخ‌های نمایشی همیشه شمسی باشند.** داده‌ی تاریخ به‌صورت میلادی/UTC
   ذخیره می‌شود (برای محاسبه‌ی دقیق)، ولی در نمایش **همیشه** شمسی شود:
   - فرانت: از `faDate` (بدون ساعت) یا `faDateTime` (با ساعت) استفاده کن — هرگز تاریخِ
     خامِ ISO/میلادی را مستقیم نشان نده. (تقویم شمسی داخلِ مرورگر است: `Intl` با `fa-IR`.)
   - بک‌اند: برای برچسبِ شمسی از `src/shared/utils/jalali.py` استفاده کن.
9. **همیشه RTL و فارسی را رعایت کن.** کلِ UI راست‌چین فارسی است (`dir="rtl"` در layout).
   فقط داده‌های ذاتاً لاتین (شماره موبایل، مبلغ، کد رهگیری) می‌توانند `dir="ltr"` باشند؛
   تاریخ/متنِ فارسی هرگز نباید `ltr` شود. اعداد در نمایش با `faNum`/`faDigits` فارسی شوند.

---

## 📦 معماری پروژه (ماژولار)

```
backend/  → FastAPI، Clean Architecture، ۶ ماژول مستقل:
            identity (کاربر/نقش/JWT/RBAC)
            crm (دانشجو/دوره/تگ/یادداشت/پیگیری/مراحل فروش)
            telephony (تماس/ضبط/وب‌هوک + Provider قابل‌تعویض)
            ai_analysis (LangGraph + Whisper + GPT-5.5 + امتیازدهی)
            analytics (داشبورد)
            assistant (دستیار چت)
frontend/ → Next.js 15، RTL فارسی، داشبورد گرافیکی
docs/     → ۱۲ سند معماری فارسی
```

هر ماژول لایه‌بندی شده: `domain/` → `application/` (+ports) → `infrastructure/` → `api/`.
**قانون:** دامنه به فریم‌ورک وابسته نیست؛ Providerها پشت اینترفیس (Open/Closed).

> 📖 **مرجع کلیدی:** [`docs/11-CODE-GUIDE.md`](docs/11-CODE-GUIDE.md) — راهنمای مرور کدِ
> فایل‌به‌فایل (هر فایلِ بک‌اند/فرانت چه‌کار می‌کند + توابع کلیدی). قبل از کار روی هر
> فایل، اول این را ببین. **هر فایل/تابع جدید را همان‌جا ثبت کن** (قانون طلایی #۷).

---

## ✅ کارهای انجام‌شده (به ترتیب)

1. **معماری کامل + اسکلت** — ۹ سند فارسی، ساختار ماژولار، ERD، LangGraph.
2. **پیاده‌سازی واقعی** — سرویس داشبورد، تماس‌ها، CRM (courses/tags/stages)،
   ذخیره transcript، Audit log، Rate limiting، seed، ۱۸ تست.
3. **migration اولیه** — ساخت خودکار جدول‌ها از `sql/schema.sql` + seed در فاز release پارس‌پک.
4. **اتصال سیموتل** — `SimotelProvider` (رویدادهای IncomingCall/OutgoingCall/CDRQueue)،
   endpoint `/webhooks/simotel`، ۱۰ تست. بدون تغییر دامنه (لطف معماری ماژولار).
5. **داشبورد گرافیکی زیبا** — StatCard رنگی، نمودار Area روند تماس، دونات سرنخ،
   قیف فروش، فونت Vazirmatn، Sidebar گرادیانتی. endpoint `calls-trend`.
6. **حالت دمو فرانت** — `frontend/src/lib/demoData.ts` + سوییچ در `api.ts`
   (`NEXT_PUBLIC_DEMO=1`). داده‌ی فیک واقعی (رشته‌ی کنکوری تجربی/ریاضی/انسانی،
   پایه، هدف دانشگاهی) برای دیدن ظاهر بدون دیتابیس. هر وقت دیتابیس آمد، خاموش می‌شود.
7. **شماره موبایل فقط از سیموتل** — تأیید شد که موبایل از `event.caller_number` می‌آید
   نه از AI؛ مدل `ExtractedInfo` عمداً فیلد mobile ندارد. کامنت محافظ گذاشته شد.
8. **نشان اطمینان AI** — فیلد `confidence` در `ExtractedInfo` + نشان رنگی در صفحه‌ی
   تماس‌ها (سبز/زرد/قرمز=نیاز به بازبینی) + دکمه‌ی ویرایش (ظاهر؛ منطقش با دیتابیس وصل می‌شود).
9. **زیباسازی + یوزرفرندلی** — صفحه‌ی دانشجویان و تماس‌ها: جستجو، فیلتر، آواتار،
   مرتب‌سازی بر اساس امتیاز، کارت‌های آماری، حالت خالی، ته‌رنگ پاستیلی، راهنمای امتیاز سرنخ.
10. **تم تیره سرمه‌ای** — پس‌زمینه `#334766` (در `globals.css`)، سرتیترها سفید.
11. **ریسپانسیو کامل (موبایل/دسکتاپ، اندروید/iOS)** — viewport در layout،
    Sidebar با منوی همبرگری روی موبایل، چیدمان `flex-col md:flex-row`،
    پدینگ `p-4 md:p-8`، جدول‌ها `overflow-x-auto` + `min-w`.
12. **دکمه‌ی برگشت RTL** — `BackButton.tsx` (فلش راست، فارسی) در همه‌ی صفحه‌ها.
13. **PWA** — قابل نصب روی اندروید/iOS/دسکتاپ (`ServiceWorkerRegister.tsx`).
14. **سخت‌سازی پروداکشن** — Health Check واقعی (`/healthz`/`/readyz`)، کش داشبورد،
    rate limit عمومی + gzip + statement_timeout.
15. **ورود با پیامک (OTP)** — جریان امن: کد ۵رقمی، هش HMAC در Redis (TTL=۱۲۰ث)،
    مقایسه‌ی time-constant، سقف ۵ تلاش، cooldown=۶۰ث + Auth Guard صفحات.
16. **پنل مدیریت کاربران** — افزودن/ویرایش/فعال‌سازی اعضای تیم (RBAC؛ کاربرِ فقط-OTP
    با ایمیل داخلی `{mobile}@otp.local`).
17. **ثبت فیش فروش واقعی** — جدول `sales` + لیست محصولات + مدت برنامه؛ محصول «برنامه»
    الزاماً `program_months` دارد (validator) + پیگیری خودکار +۵ روز پس از فروش.
18. **صفحه‌ی اصلی «کارهای روز»** — هاب روزانه‌ی نیرو (پیگیری‌های امروز، تماس‌های بدون
    اقدام، ازدست‌رفته، یادآور تمدید) به‌عنوان صفحه‌ی ورودی (`/tasks`).
19. **معدل + گزارش‌ها + پیام‌ها (commit `0b77153`)** —
    - ستون `gpa` در دانشجو + جدول `messages` (migration `0005`، idempotent).
    - `MessagingService`: پیامک/واتساپ/تلگرام؛ پیامک واقعاً ارسال می‌شود، واتساپ/تلگرام
      فقط لاگ می‌شوند (ارسال سمت کلاینت با `ContactLinks.tsx`).
    - **رفع گپ ملی‌پیامک:** `send_text` به `MelipayamakSmsProvider` اضافه شد تا پیامکِ
      متنِ آزاد واقعاً ارسال شود (قبلاً به‌اشتباه فقط لاگ می‌شد و status گمراه‌کننده «sent» می‌خورد).
    - گزارش‌های پیشرفته: عملکرد ماهانه (ماه مالی شمسی)/روزانه/ساعتی، داده‌ی ناقص، تایم‌لاین ورود→خرید.
    - `shared/utils/jalali.py`: تبدیل میلادی↔شمسی + منطق ماه مالی (بدون کتابخانه‌ی بیرونی).
20. **تطبیق با چک‌لیست کارفرما (commit `e5715f2`)** — سه مورد جامانده رفع شد:
    - 🐞 **باگ ثبت ناقص دانشجو:** `create()` فیلدهای شهر/رشته/پایه/هدف/معدل را دور می‌ریخت
      (فقط زیرمجموعه‌ای را پاس می‌داد)؛ حالا کامل ذخیره می‌شود. ⚠️ الگوی هشدار: هر فیلد
      جدیدِ `StudentCreate` باید **هم** در سازنده‌ی `Student` داخل `create()` اضافه شود.
    - 📞 **تعداد تماس هر فرد:** subquery شمارش تماس در `StudentService.list` (کنار نام).
    - 🔁 **مشتریان چندبارخرید:** `GET /sales/repeat-customers` (تعداد خرید + تاریخ هر خرید
      + فاصله‌ی روز بین خریدها + جمع کل) + بخش جدید در صفحه‌ی گزارش‌ها + داده‌ی دمو.
    - ✅ تأییدشده: دسته‌بندی (Grade×field)، وضعیت تماس (۵ حالت)، منبع تماس (۷ گزینه) دقیقاً
      مطابق فهرست کارفرما هستند. گزارش روز/فروش، جمع فروش، جزئیات واریز کامل‌اند.
21. **سند مرور کد فایل‌به‌فایل** — [`docs/11-CODE-GUIDE.md`](docs/11-CODE-GUIDE.md) ساخته شد
    (نقش هر فایلِ بک‌اند/فرانت + توابع کلیدی). افزودنی محض؛ چیزی حذف نشد.
22. **گزارش ارتباطات + بررسی آمار ساعتی (commitهای `6044856`/`a325f03`)** —
    - **جستجوی شخص** در گزارش ارتباطات (نام/موبایل، سمت کلاینت) + شمارنده + خروجی هماهنگ.
      بک‌اند از قبل بازه‌ی تاریخ و `student_id` را پشتیبانی می‌کرد. تأییدشد: گزارش اطلاعات
      ناقص (`list_incomplete`) مو‌به‌مو مطابق فهرست کارفرماست (نام/پایه/رشته/هدف/معدل/پیامک/
      واتساپ‌تلگرام/توضیحات).
    - **آمار ساعتی** (به وقت تهران): بیشترین واریز/پاسخ/بی‌پاسخ + پراقدام‌ترین ساعت + میانگین
      مکالمه، کلی و تفکیک نیرو، تیتر‌تیتر + نمودار.
    - 🐞 **باگِ رفع‌شده:** «میانگین مکالمه» با انتخاب نیرو فیلتر نمی‌شد (میانگین کلِ تیم را
      می‌داد)؛ حالا با `agent_id` فیلتر می‌شود. ⚠️ الگو: هر متریکِ تفکیک‌نیرو باید فیلتر
      `agent_id` را اعمال کند، نه فقط آرایه‌های ساعتی.
23. **بررسی/رفع صفحه‌ی «کارهای روز» (commit `a76c9e8`)** — همه‌ی خواسته‌های کارفرما از
    قبل بود (تماس بدون اقدامِ روزهای گذشته، بی‌پاسخ‌ها، پیگیری امروز، باکس شماره‌ی جدید)؛
    سه ایراد رفع شد:
    - 🐞 **باگ اصلی:** `tasks_today(owner_id)` و `calls_missing_next_call` پارامتر owner را
      می‌گرفتند ولی فیلتر نمی‌کردند → هر کارشناس کارهای کلِ تیم را می‌دید. حالا کارشناس فقط
      کارهای خودش (پیگیری/تماس خروجی/تمدید)؛ تماس‌های **بی‌صاحب** (`agent_id` خالی، ورودی)
      برای همه می‌ماند تا گم نشود؛ مدیر/ادمین کلِ تیم (`_task_scope` در routes).
      ⚠️ الگو: پارامتر owner_id باید واقعاً در where اعمال شود.
    - 🐞 **منطقه‌ی زمانی:** مرز «امروز» از UTC به تهران منتقل شد (`_start_of_today_tehran`،
      آفست ثابت +۳:۳۰ چون ایران DST ندارد) تا بامداد «دیروز» نشان داده نشود.
    - **ناوبری:** لینک یادآور از `<a>` به `<Link>` Next تبدیل شد (بدون reload کامل).

**کامپوننت‌های فرانت کلیدی:** StatCard, ChartCard, CallButton, ScoreLegend,
BackButton, Sidebar, ContactLinks, Pagination, ExportButton/ExportAllButton.
**نکته:** این یک وب‌اپ ریسپانسیو (PWA قابل‌نصب) است، نه اپ نیتیو.

**وضعیت تست‌ها:** ۲۸ تست بک‌اند — همه سبز ✅
**وضعیت کامپایل:** بک‌اند بدون خطا، فرانت typecheck پاس (exit 0).

> ⚠️ **گپِ باقی‌مانده (برای آگاهی):** `ConsoleSmsProvider` متد `send_text` را override نکرده
> (از پیش‌فرضِ لاگ‌محورِ `ports.py` استفاده می‌کند) — این عمدی و درست است (حالت تست).
> فقط در حالت `sms_provider=melipayamak` ارسال واقعی رخ می‌دهد.

---

## 🔑 وضعیت تنظیمات (.env در backend/)

- ✅ `AVALAI_API_KEY` — ثبت شده (کلید AvalAI، در `backend/.env`)
- ✅ `TELEPHONY_PROVIDER=simotel`
- ⏳ `DATABASE_URL` — منتظر آدرس PostgreSQL پارس‌پک (کاربر بعداً می‌سازد)
- ⏳ `REDIS_URL` — منتظر آدرس Redis پارس‌پک
- ⏳ سیموتل (API base/token) — منتظر اطلاعات از کاربر

> ⚠️ فایل `crm/.env` (ریشه) مالِ پروژه‌ی دیگری (rankiiup) است و کلیدهای واقعی دارد.
> دست نزن. هر دو `.env` در `.gitignore` هستند و در گیت commit نمی‌شوند (تأییدشده).
> حالت دمو فعال است (`NEXT_PUBLIC_DEMO=1` در `frontend/.env.local`) تا بدون دیتابیس ظاهر دیده شود.

---

## 🚀 قدم بعدی

استقرار روی پارس‌پک (راهنما: `docs/09-PARSPACK-STEP-BY-STEP.md`).
کاربر باید: حساب GitHub + پارس‌پک، ساخت PostgreSQL و Redis، وارد کردن متغیرها در پنل.

---

## 📜 تاریخچه‌ی Commitها

```
a76c9e8 «کارهای روز من» per-agent + مرز روز تهرانی + ناوبری Next
c6b7200 ثبت جستجوی شخص/آمار ساعتی در MEMORY
a325f03 رفع باگ میانگین مکالمه (تفکیک نیرو) + تیتر پراقدام‌ترین ساعت تماس
6044856 جستجوی شخص در گزارش ارتباطات + شمارنده
6612b13 افزودن راهنمای مرور کد فایل‌به‌فایل (docs/11-CODE-GUIDE.md)
e5715f2 رفع باگ ثبت ناقص دانشجو + تعداد تماس + گزارش مشتریان چندبارخرید
dff6f4d به‌روزرسانی MEMORY.md
0b77153 معدل + گزارش پیام‌ها + گزارش‌های پیشرفته عملکرد + رفع ارسال پیامک متنی
ddf268a صفحه‌ی اصلی «کارهای روز» نیرو
c3ea677 فاز ۱ — ثبت فیش فروش واقعی (جدول sales + لیست محصولات + مدت برنامه)
1d225bf پنل مدیریت کاربران (افزودن/ویرایش/فعال‌سازی اعضای تیم)
e2b802b ورود با پیامک (OTP) + محافظت صفحات (Auth Guard)
2a1a8cd سخت‌سازی نهایی پروداکشن — rate limit عمومی + gzip + statement_timeout
c965b09 سخت‌سازی پروداکشن — Health Check واقعی + کش داشبورد
637d2ee تبدیل فرانت به PWA (قابل نصب روی اندروید/آیفون/دسکتاپ)
... (تاریخچه‌ی کامل: git log)
3f67073 داشبورد گرافیکی زیبا + endpoint روند تماس‌ها
8767b8b افزودن SimotelProvider
130c5c0 migration اولیه + آماده‌سازی استقرار پارس‌پک
b114ae1 تکمیل پیاده‌سازی واقعی سرویس‌ها، امنیت و تست‌ها
27677c1 معماری کامل و اسکلت پیاده‌سازی
```
